This API will manage, handle and log MCP Server sessions and actions.

Logging -> Currently logs with syslog, will add more options in the future.
Session -> Handles FastMCP sessions (mcp_manager > MCPManager class)
API -> main.py
